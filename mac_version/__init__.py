"""mac_version package marker for network_monitoring simulator and modules.

This file makes the `mac_version` directory a Python package so the
simulator script can import `mac_version.jetson_network_monitor` when
run as a module (python -m mac_version.simulate_run).
"""

__all__ = []
